[Unit]
Description=HERO2 guard for Docker-published database ports
After=docker.service ufw.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/hero2-docker-guard
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
